minimum_pre_commit_version: 4.2.0

repos:
  - repo: local
    hooks:
      - id: swift-format
        name: swift-format
        entry: swift format --in-place
        language: system
        types: [swift]
        require_serial: true
